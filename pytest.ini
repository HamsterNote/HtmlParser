[pytest]
testpaths = e2e
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: 快速冒烟测试（核心流程验证）
    regression: 回归测试（确保已有功能不被破坏）
    e2e: 端到端浏览器测试（完整用户流程）
addopts = -ra --tb=short --strict-markers